<odoo>
    <record id="action_customer_ledger_report" model="ir.actions.act_window">
        <field name="name">Customer Ledger Report</field>
        <field name="res_model">customer.ledger.report</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_sm">Generate customer ledger reports.</p>
        </field>
    </record>

    <menuitem id="customer_ledger_main_menu" name="Customer Ledger" parent="account.menu_finance" sequence="10"/>

    <menuitem id="customer_ledger_report_menu"
              name="Customer Ledger Report"
              parent="customer_ledger_main_menu"
              action="action_customer_ledger_report"/>
</odoo>
